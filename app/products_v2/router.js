// const router = require('express').Router();
// const multer = require('multer')
// const upload = multer({dest: 'uploads'})
// const productV2Controller = require('./controller')

// router.get('/product', upload.single('image'),productV2Controller.view)
// router.get('/product/:id', upload.single('image'),productV2Controller.view)
// router.post('/product', upload.single('image'),productV2Controller.create)
// router.put('/product/:id',upload.single('image'), productV2Controller.update)   
// router.delete('/product/:id',upload.single('image'), productV2Controller.destroy)   

// module.exports = router;